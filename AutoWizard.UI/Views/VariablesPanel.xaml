<UserControl x:Class="AutoWizard.UI.Views.VariablesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:AutoWizard.Core.Models;assembly=AutoWizard.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="260">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FAFAFA" Padding="10,8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="📦 變數管理" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="＋" ToolTip="新增變數"
                            Command="{Binding AddVariableCommand}"
                            Padding="6,2" FontSize="13" Margin="2,0"
                            Background="#E8F5E9" BorderBrush="#A5D6A7" Cursor="Hand"/>
                    <Button Content="📋" ToolTip="複製所選變數"
                            Command="{Binding DuplicateVariableCommand}"
                            Padding="6,2" FontSize="13" Margin="2,0"
                            Background="#E3F2FD" BorderBrush="#90CAF9" Cursor="Hand"/>
                    <Button Content="✕" ToolTip="刪除所選變數"
                            Command="{Binding DeleteVariableCommand}"
                            Padding="6,2" FontSize="13" Margin="2,0"
                            Background="#FFEBEE" BorderBrush="#EF9A9A" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Variable Count -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="10,4" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
            <TextBlock Foreground="#9E9E9E" FontSize="11">
                <TextBlock.Text>
                    <MultiBinding StringFormat="共 {0} 個變數">
                        <Binding Path="Variables.Count"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Border>

        <!-- Variables List -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Variables}" SelectedItem="{Binding SelectedVariable, Mode=TwoWay}"
                 Padding="4" BorderThickness="0" Background="Transparent" 
                 HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                        <Border Margin="0,2" Padding="8,6" Background="#FFFFFF" BorderBrush="#E0E0E0" 
                                BorderThickness="1" CornerRadius="4">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="BorderBrush" Value="#2196F3"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Setter Property="Background" Value="#F3F9FF"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Name -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="名稱" 
                                           FontSize="10" Foreground="#757575" VerticalAlignment="Center"/>
                                <Grid Grid.Row="0" Grid.Column="1" Margin="0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" 
                                             Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Padding="4,2" BorderBrush="#E0E0E0" FontSize="12" FontWeight="SemiBold"/>
                                    <Button Grid.Column="1" Content="🎨" ToolTip="特定顏色取色"
                                            Command="{Binding DataContext.PickSpecificColorCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Padding="6,0" FontSize="12" Margin="4,0,0,0"
                                            Background="#FFF3E0" BorderBrush="#FFCC80" Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsBuiltInColor}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>

                                <!-- Type -->
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="類型" 
                                           FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,4,0,0"/>
                                <ComboBox Grid.Row="1" Grid.Column="1"
                                          SelectedItem="{Binding Type, Mode=TwoWay}"
                                          Padding="4,2" FontSize="11" Margin="0,4,0,0">
                                    <models:VariableType>String</models:VariableType>
                                    <models:VariableType>Integer</models:VariableType>
                                    <models:VariableType>Double</models:VariableType>
                                    <models:VariableType>Boolean</models:VariableType>
                                </ComboBox>

                                <!-- Default Value -->
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="預設值" 
                                           FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBox Grid.Row="2" Grid.Column="1"
                                         Text="{Binding DefaultValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="4,2" BorderBrush="#E0E0E0" FontSize="11" Margin="0,4,0,0"/>

                                <!-- Description -->
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="說明" 
                                           FontSize="10" Foreground="#757575" VerticalAlignment="Center" Margin="0,4,0,0"/>
                                <TextBox Grid.Row="3" Grid.Column="1"
                                         Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="4,2" BorderBrush="#E0E0E0" FontSize="11" Foreground="#757575"
                                         Margin="0,4,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Variables.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="📦" FontSize="28" HorizontalAlignment="Center" Foreground="#E0E0E0" Margin="0,0,0,8"/>
            <TextBlock Text="尚無變數" Foreground="#BDBDBD" FontSize="12" TextAlignment="Center"/>
            <TextBlock Text="點擊 ＋ 按鈕新增變數" 
                       Foreground="#E0E0E0" FontSize="10" TextAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
