<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:AutoWizard.UI.Controls">
    
    <Style TargetType="{x:Type local:ActionBlock}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ActionBlock}">
                    <Border Background="{TemplateBinding ActionColor}"
                            BorderBrush="#333333"
                            BorderThickness="2"
                            CornerRadius="5"
                            Padding="10"
                            Margin="5"
                            MinWidth="200"
                            Cursor="Hand">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="5" ShadowDepth="2"/>
                        </Border.Effect>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                        <Setter Property="BorderBrush" Value="#FF9800"/>
                                        <Setter Property="BorderThickness" Value="3"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        
                        <StackPanel>
                            <TextBlock Text="{TemplateBinding ActionTitle}" 
                                       FontWeight="Bold" 
                                       FontSize="14"
                                       Foreground="White"/>
                            <ContentPresenter Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
